/**
 * All locales used in the app
 */
export enum Locale {
  'de-DE' = 'de-DE',
  'en-DE' = 'en-DE',
}

export enum Language {
  'de' = 'de',
  'en' = 'en',
}

export const defaultLanguage = Language.de;
