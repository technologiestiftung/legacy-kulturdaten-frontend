import { NextPage } from 'next';

import { AppWrapper } from '../../../components/wrappers/AppWrapper';

const SettingsPage: NextPage = () => {
  return <AppWrapper>User Settings</AppWrapper>;
};

export default SettingsPage;
